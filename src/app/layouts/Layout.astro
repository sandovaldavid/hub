---
import '@shared/styles/global.css';
import { getThemeInitScript } from '@entities/theme/lib/theme-manager';
import ShareButton from '@features/share-button/ui/ShareButton.astro';
import ThemeToggle from '@features/theme-toggle/ui/ThemeToggle.astro';
import LanguageToggle from '@features/language-toggle/ui/LanguageToggle.astro';
import SiteFooter from '@widgets/site-footer/ui/SiteFooter.astro';
import type { SEOProps } from '@data/seo';
import Analytics from '@vercel/analytics/astro';
import { siteConfig } from '@data/site.config';
import { getProfilePageStructuredData } from '@data/structured-data';
import { useTranslations } from '@shared/i18n';

interface Props extends SEOProps {
	alternates?: { en: string; es: string };
}

const {
	title,
	description,
	author,
	ogImage,
	themeColor,
	applicationName = siteConfig.name,
	appleMobileWebAppTitle = siteConfig.shortName,
	ogType = 'profile',
	ogImageAlt = '',
	ogImageSecureUrl,
	ogImageType = 'image/png',
	ogImageWidth,
	ogImageHeight,
	ogSiteName = siteConfig.name,
	fbAppId = siteConfig.fbAppId,
	ogLocale = 'en_US',
	twitterCard = 'summary_large_image',
	twitterSite = siteConfig.twitterHandle,
	twitterCreator,
	twitterImageAlt = '',
	twitterLabel1 = '',
	twitterData1 = '',
	robots = 'index, follow',
	googlebot = 'index, follow',
	keywords = [],
	lang = 'en',
	alternates,
} = Astro.props as Props;

const t = useTranslations(lang);
const themeScript = getThemeInitScript();
const keywordsContent = keywords.length > 0 ? keywords.join(', ') : undefined;
const finalCanonicalUrl = new URL(Astro.url.pathname, siteConfig.url).href;
const sitemapUrl = new URL('/sitemap-index.xml', siteConfig.url).href;
const absoluteOgImage = new URL(ogImageSecureUrl ?? ogImage, siteConfig.url).href;
const profileImageAlt = ogImageAlt || twitterImageAlt || `${author} professional profile`;
const alternateLocale = ogLocale.startsWith('es_') ? 'en_US' : 'es_PE';
const jsonLd = getProfilePageStructuredData({
	title,
	description,
	lang,
	canonicalUrl: finalCanonicalUrl,
	imageUrl: absoluteOgImage,
	imageAlt: profileImageAlt,
});
---

<!doctype html>
<html {lang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />
		<link rel="sitemap" href={sitemapUrl} />

		<title>{title}</title>
		<meta name="title" content={title} />
		<meta name="description" content={description} />
		<meta name="author" content={author} />
		{keywordsContent && <meta name="keywords" content={keywordsContent} />}
		<meta name="application-name" content={applicationName} />
		<meta name="apple-mobile-web-app-title" content={appleMobileWebAppTitle} />
		<meta name="theme-color" content={themeColor} />
		<meta name="color-scheme" content="light dark" />
		<link id="site-favicon" rel="icon" type="image/svg+xml" href="/favicon.dark.svg" />

		<meta property="og:type" content={ogType} />
		<meta property="og:url" content={finalCanonicalUrl} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={absoluteOgImage} />
		<meta property="og:image:secure_url" content={absoluteOgImage} />
		<meta property="og:site_name" content={ogSiteName} />
		<meta property="og:author" content={author} />
		{ogImageAlt && <meta property="og:image:alt" content={ogImageAlt} />}
		{ogImageType && <meta property="og:image:type" content={ogImageType} />}
		{ogImageWidth && <meta property="og:image:width" content={String(ogImageWidth)} />}
		{ogImageHeight && <meta property="og:image:height" content={String(ogImageHeight)} />}
		{fbAppId && <meta property="fb:app_id" content={fbAppId} />}
		<meta property="og:locale" content={ogLocale} />
		<meta property="og:locale:alternate" content={alternateLocale} />

		<meta name="twitter:card" content={twitterCard} />
		<meta name="twitter:url" content={finalCanonicalUrl} />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={absoluteOgImage} />
		<meta name="twitter:site" content={twitterSite} />
		{twitterCreator && <meta name="twitter:creator" content={twitterCreator} />}
		{twitterImageAlt && <meta name="twitter:image:alt" content={twitterImageAlt} />}
		{twitterLabel1 && <meta name="twitter:label1" content={twitterLabel1} />}
		{twitterData1 && <meta name="twitter:data1" content={twitterData1} />}

		{
			alternates && (
				<>
					<link rel="alternate" hreflang="en" href={new URL(alternates.en, siteConfig.url)} />
					<link rel="alternate" hreflang="es" href={new URL(alternates.es, siteConfig.url)} />
					<link
						rel="alternate"
						hreflang="x-default"
						href={new URL(alternates.en, siteConfig.url)}
					/>
				</>
			)
		}

		<link rel="canonical" href={finalCanonicalUrl} />
		<meta name="robots" content={robots} />
		<meta name="googlebot" content={googlebot} />
		<meta name="mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

		<script is:inline set:html={themeScript} />
		<script>
			import { ThemeManager } from '@entities/theme/lib/theme-manager';
			import { updateFavicon } from '@entities/theme/lib/theme-assets';

			window.addEventListener('theme-change', event => {
				const effectiveTheme = (event as CustomEvent<{ effective?: string }>).detail?.effective;
				if (effectiveTheme === 'light' || effectiveTheme === 'dark') updateFavicon(effectiveTheme);
			});

			document.addEventListener('astro:after-swap', () => {
				updateFavicon(new ThemeManager().getThemeState().effective);
			});
		</script>
		<script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
	</head>
	<body class="min-h-screen bg-channel-background-canvas text-channel-content-default">
		<a href="#main-content" class="skip-link">{t('nav.skipToContent')}</a>
		<ShareButton {lang} />
		<LanguageToggle {lang} {alternates} />
		<ThemeToggle {lang} />
		<slot />
		<SiteFooter {lang} />
		<Analytics />
		<script>
			import { initConversionAnalytics } from '@shared/analytics/conversion';
			initConversionAnalytics();
		</script>
	</body>
</html>
