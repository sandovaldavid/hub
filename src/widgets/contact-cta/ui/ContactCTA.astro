---
import Card from '@shared/ui/Card.astro';
import Button from '@shared/ui/Button.astro';
import type { ContactCTAProps } from '../model/types';
import { cn } from '@shared/lib/utils';
import ChatIcon from '@shared/assets/cta-icons/chat.svg?raw';
import SentIcon from '@shared/assets/sent.svg';

export type Props = ContactCTAProps;

const {
	title = '¿Tienes un proyecto en mente?',
	message = 'Hablemos sobre cómo puedo ayudarte a alcanzar tus objetivos',
	buttonText = 'Hablemos',
	email,
	subject = 'Consulta desde Hub Personal',
	variant = 'default',
} = Astro.props;

const mailtoLink = `mailto:${email}?subject=${encodeURIComponent(subject)}`;
---

<Card
	variant={variant === 'prominent' ? 'gradient' : 'default'}
	padding="none"
	class={cn(
		'contact-cta h-full w-full relative overflow-hidden flex',
		'bg-white/80 backdrop-blur-lg',
		'border-2 border-primary-100',
		'rounded-2xl shadow-lg',
		'dark:bg-neutral-900/30 dark:border-primary-700'
	)}>
	<div
		class={cn(
			'flex h-full flex-1 flex-col items-center justify-center gap-5 px-6 py-8 text-center min-w-0',
			'sm:px-10 md:px-8 lg:px-10'
		)}>
		<div
			class={cn(
				'flex items-center justify-center rounded-full',
				'bg-primary-100 w-12 h-12 md:w-14 md:h-14 lg:w-16 lg:h-16 shadow-md',
				'dark:bg-primary-950'
			)}
			aria-hidden="true">
			<span
				class="inline-block h-8 w-8 text-primary-600 md:h-9 md:w-9 lg:h-10 lg:w-10 dark:text-primary-300"
				set:html={ChatIcon}
			/>
		</div>
		<div class="flex max-w-xl flex-col gap-2">
			<h2
				id="contact-heading"
				class={cn(
					'mb-1 text-lg font-bold leading-tight md:text-xl lg:text-2xl',
					'text-neutral-900 dark:text-neutral-100'
				)}>
				{title}
			</h2>
			{
				message && (
					<p
						class={cn(
							'text-sm leading-snug md:text-base lg:text-base',
							'text-neutral-700 dark:text-neutral-300'
						)}>
						{message}
					</p>
				)
			}
		</div>
		<a
			href={mailtoLink}
			class="w-full max-w-xs"
			data-conversion-event="contact_clicked"
			data-conversion-position="contact"
			data-conversion-item="email"
			aria-label={`Enviar correo a ${email}: ${subject}`}>
			<Button
				variant="primary"
				size="lg"
				class="flex w-full cursor-pointer items-center justify-center gap-2 font-semibold">
				<span>{buttonText}</span>
				<SentIcon class="h-6 w-6" aria-hidden="true" />
			</Button>
		</a>
	</div>
</Card>
